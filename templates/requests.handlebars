<h1> Requests </h1>

<h2> From others: </h2>
{{#each requestsToUser}}
  <div id={{_id}}><a id={{_id}}>{{name}}</a> <button id='confirm' type='button'>Confirm</button> <button id='deny' type='button'>Deny</button></div>
{{/each}}

<h2> From you: </h2>
{{#each requestsFromUser}}
	{{!TODO: fix link!}}
	<div id={{_id}}><a id={{_id}}>{{name}}</a> <button id="cancel" type="button">Cancel</button></div>
{{/each}}